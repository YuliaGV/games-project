import { Container } from "@mui/material"

function NotFoundPage() {
  return (
    <Container maxWidth="xl" className="container">
      Error: El recurso al que intentas acceder no existe
    </Container>
  )
}

export default NotFoundPage